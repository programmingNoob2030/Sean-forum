# 测试范围

本次验证覆盖 `rich-text-support` 的需求、设计和任务拆分要求，重点看四类内容：

- 评论回归：普通评论、空白评论、emoji-only 评论、回复、删除恢复、消息通知、评论计数
- 帖子正文：历史纯文本、新结构化正文、emoji、换行、图片展示、非法内容拒绝
- 图片上传：格式校验、非法文件、超大小文件、上传失败处理
- 页面回归：发帖、帖子详情、帖子列表、最近浏览

# 测试环境

- 操作系统：Windows
- 后端：Java 17 + Maven
- 前端：Vue 3 + Vite 8 + TypeScript
- 运行方式：使用工作区自带 Node/pnpm 组件完成前端构建
- 说明：本机全局 `npm` 不可用，前端验证改用工作区内置依赖

# 测试结果

- `PostServiceContentTest`：通过，6/6
- `FileUploadServiceTest`：通过，4/4
- `CommentServiceTest`：未通过，Spring 容器启动失败
- 前端类型检查：通过
- 前端打包：通过

# 测试用例

1. 帖子正文纯文本兼容
   - 结果：通过
   - 说明：旧纯文本可正常保存和读取，正文预览保留原文。

2. 结构化正文解析
   - 结果：通过
   - 说明：`text` 和 `image` 节点可正常解析，首图和正文预览可提取。

3. 非法正文拒绝
   - 结果：通过
   - 说明：外部 URL 图片节点、超长纯文本等都会在入库前拒绝。

4. 图片上传
   - 结果：通过
   - 说明：`jpg/jpeg/png` 可上传；伪造内容、扩展名不匹配、超 10MB 均拒绝。

5. 评论空白与 emoji-only
   - 结果：部分通过
   - 说明：空白评论拒绝逻辑已在单测中覆盖；emoji-only 评论的集成回归未完成，因为 `CommentServiceTest` 无法启动。

6. 页面构建
   - 结果：通过
   - 说明：`vue-tsc -b` 和 `vite build` 均通过，说明前端编辑器、渲染器和详情页能完成编译。

# 发现问题

1. `CommentServiceTest` 启动失败
   - 原因：Spring 上下文初始化时缺少 `file.upload-path` 配置，占位符解析失败。
   - 影响：评论联调回归无法进入真正的业务断言阶段。
   - 建议：为测试环境补齐 `file.upload-path`，或给 `WebConfig` 增加测试可用的默认配置。

# 风险说明

- 当前评论回归只完成了代码级空白/emoji 规则确认，未完成完整 Spring 集成验证。
- 前端已通过构建，但未做真实浏览器点击回归，因此图片加载失败占位、列表首图展示等仍属于构建级确认。
- 依赖环境曾出现本机 `npm` 和前端包解析异常，后续回归最好固定使用同一套工作区运行时。

# 验收结论

`rich-text-support` 的帖子正文解析、图片上传和前端构建已通过验证；评论回归仍受测试环境配置阻塞，暂不建议判定为完全验收通过。当前结论是：主体功能基本满足需求，评论联调补齐后可进入最终验收。
